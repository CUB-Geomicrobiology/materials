# start guarding with 'bundle exec guard'
# to host locally at specific IP use 'sudo jekyll serve -H <IP> -P 80' instead
guard 'jekyll-plus', :serve => true do
  watch /.*/
  ignore /^_site/
end

guard 'livereload' do
  watch /.*/
end
